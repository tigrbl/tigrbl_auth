schema_version: 1
package: tigrbl_auth
summary:
  issue_count: 1
issues:
- id: release.final-certification-blocked
  severity: high
  status: open
  description: The repository is still a truthful checkpoint and not a final certified
    release.
  blockers:
  - Tier 4 independent peer validation is not complete for the retained boundary.
  - One or more supported peer profiles have incomplete or invalid preserved external
    evidence bundles.
  - At least one kept runner is missing declared clean-room CI install/probe coverage.
  - "Migration upgrade \u2192 downgrade \u2192 reapply portability has not been preserved\
    \ for both SQLite and PostgreSQL."
  - One or more operator-visible package capabilities still lacks end-to-end verification
    in the current environment.
  - Release evidence can now be built only from a clean checkout, and the current
    workspace is dirty.
