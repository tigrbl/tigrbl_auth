# tigrbl-replay-protection

Protocol-neutral replay reservation provider with capability reporting over an
injected reservation backend. It accepts normalized keys and does not know
protocol wire fields.
