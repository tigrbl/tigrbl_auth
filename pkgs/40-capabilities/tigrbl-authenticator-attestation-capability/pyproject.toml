[project]
name = "tigrbl-authenticator-attestation-capability"
version = "0.4.0.dev2"
description = "Composable authenticator attestation appraisal capability."
readme = "README.md"
requires-python = ">=3.10,<3.15"
license = "Apache-2.0"
dependencies = ["tigrbl-capability==0.4.0.dev2"]
[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
[tool.poetry]
packages = [{ include = "tigrbl_authenticator_attestation_capability", from = "src" }]
