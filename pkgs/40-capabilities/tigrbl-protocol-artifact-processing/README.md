# tigrbl-protocol-artifact-processing

Mountable protocol-neutral capability for processing encoded artifacts through
an injected deterministic processor. It owns no wire schema, protocol revision,
keys, trust anchors, persistence, or deployment settings.
