# tigrbl-token-exchange-capability

Protocol-neutral, reportable security-token exchange capability.

The package coordinates a normalized exchange request and trusted execution
context through an injected implementation. RFC 8693 wire names, HTTP routing,
deployment settings, persistence, and key-provider policy remain outside it.
