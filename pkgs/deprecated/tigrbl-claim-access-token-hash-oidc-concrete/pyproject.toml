[project]
name = "tigrbl-claim-access-token-hash-oidc-concrete"
version = "0.4.0.dev2"
description = "Deprecated compatibility facade for the standalone at_hash claim."
requires-python = ">=3.10,<3.15"
dependencies = ["tigrbl-claim-access-token-hash-concrete==0.4.0.dev2"]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
packages = [{ include = "tigrbl_claim_access_token_hash_oidc_concrete", from = "src" }]
