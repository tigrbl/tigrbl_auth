# tigrbl-authn-credentials

Deprecated compatibility package. Install `tigrbl-credentials` and import
`tigrbl_credentials` for new work. The legacy `tigrbl_authn_credentials`
imports remain available for one migration window.
