[project]
name = "tigrbl-security-events"
version = "0.4.0.dev2"
description = "Deprecated alias for tigrbl-security-event-delivery-capability."
requires-python = ">=3.10,<3.15"
license = "Apache-2.0"
dependencies = ["tigrbl-security-event-delivery-capability==0.4.0.dev2"]
[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
[tool.poetry]
packages = [{ include = "tigrbl_security_events_compat", from = "src" }]
