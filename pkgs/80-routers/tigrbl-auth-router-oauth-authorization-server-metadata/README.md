# tigrbl-auth-router-oauth-authorization-server-metadata

HTTP carrier binding for OAuth 2.0 Authorization Server Metadata (RFC 8414).
Protocol semantics and runtime policy remain owned by upstream layers.
