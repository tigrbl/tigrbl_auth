# tigrbl-claim-access-token-digest-concrete

Owns the standalone RFC 9449 `ath` claim object. Protocol packages compose it
into versioned claim sets; this package does not own DPoP request processing.
