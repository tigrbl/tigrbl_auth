# tigrbl-jwt-concrete

Deterministic RFC 7519 structural decoding over compact JWS. The API is intentionally named decode_jwt_unverified; profile validation and cryptographic verification remain mandatory downstream steps.
