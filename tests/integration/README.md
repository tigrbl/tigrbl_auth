# Integration

Integration tests now live under `tests/integration/` as the canonical home for cross-module, persistence-backed, and multi-endpoint runtime flows. This test-plane checkpoint checkpoint completes the migration from the legacy `tests/i9n/` tree.
