# M2M Vendor Coverage Matrix

Research date: 2026-05-28

Scope: this matrix compares `tigrbl_auth` with Auth0, Okta, Keycloak, FusionAuth, and Amazon Cognito for machine-to-machine (M2M) and workload identity coverage. The rows intentionally go beyond OAuth `client_credentials`; a serious M2M product also needs lifecycle, credentials, authorization grants, validation, telemetry, admin surfaces, and developer setup surfaces.

| M2M capability | tigrbl_auth | Auth0 | Okta | Keycloak | FusionAuth | Amazon Cognito |
| --- | --- | --- | --- | --- | --- | --- |
| Product framing | Planned first-class M2M / workload identity lane across public issuer, resource-validation, developer, and service-admin frontdoors; see `adr:1085`, `spc:1177`, and service-admin API docs. | First-class "Machine to Machine Applications" plus API authorization using the [Client Credentials Flow](https://auth0.com/docs/flows/client-credentials-flow). | First-class service app model for Okta APIs and custom authorization servers via [OAuth for Okta service apps](https://developer.okta.com/docs/guides/implement-oauth-for-okta-serviceapp/) and [client credentials](https://developer.okta.com/docs/guides/implement-grant-type/clientcreds/main/). | Confidential clients with service accounts; M2M is modeled through clients, service-account users, roles, and protocol endpoints in [OIDC layers](https://www.keycloak.org/securing-apps/oidc-layers). | Explicit [Machine-to-machine communication](https://fusionauth.io/docs/get-started/use-cases/machine-to-machine) use case plus entity management. | Supports app-client based M2M for user pools with custom resource servers and `client_credentials`; see [resource servers](https://docs.aws.amazon.com/cognito/latest/developerguide/cognito-user-pools-define-resource-servers.html). |
| Principal / subject model | Under construction: `Service`, `ServiceKey`, `ApiKey`, `Client`, and client registration tables exist in storage/facades; product semantics need to distinguish OAuth clients from service/workload principals. | M2M application is the OAuth client; access tokens identify the client/application and API audience. | Service app is the OAuth client; Okta admin API access also uses service-app grants and admin roles. | Service account is associated with a confidential client and appears as a user-like account for role assignment. | Separates applications, users, and [entities](https://fusionauth.io/docs/get-started/core-concepts/entity-management), allowing non-user principals and relationships. | App client is the main M2M principal; no broad standalone workload identity product outside Cognito user-pool app clients. |
| Client credentials grant | Public issuer supports token issuance patterns; M2M needs hardened service/client policy around `client_credentials`. | Core supported path through [Call Your API using Client Credentials](https://auth0.com/docs/get-started/authentication-and-authorization-flow/call-your-api-using-the-client-credentials-flow). | Supported for custom authorization servers and Okta service apps through [client credentials grant](https://developer.okta.com/docs/guides/implement-grant-type/clientcreds/main/). | Supported through token endpoint for confidential clients with service accounts. | Supported as the central M2M OAuth flow. | Supported only for app clients with a client secret through Cognito [OAuth grants](https://docs.aws.amazon.com/cognito/latest/developerguide/federation-endpoints-oauth-grants.html). |
| Strong client authentication | Needs explicit policy for `client_secret_basic`, `client_secret_post`, `private_key_jwt`, mTLS, JWKS-bound clients, and rotation. | Client secret based M2M is common; Auth0 also supports private key JWT for some confidential client scenarios. | Strong here: Okta documents private key JWT and other [client authentication methods](https://developer.okta.com/docs/api/openapi/okta-oauth/guides/client-auth/). | Supports confidential client secrets and client authentication options; mTLS and client policies depend on realm configuration. | Supports application secrets, JWT signing keys, and API key patterns; M2M docs emphasize OAuth credentials. | Client credentials requires a client secret; advanced client assertion and mTLS style workload auth are limited compared with dedicated IAM-style products. |
| API / resource server model | Resource-validation API is separated from public issuer, which is a good foundation; API/resource objects need first-class M2M grants. | APIs are explicit resource servers with permissions/scopes and M2M authorization. | Custom authorization servers and scopes are the resource API model; see [API Access Management](https://developer.okta.com/docs/concepts/api-access-management/). | Clients, client scopes, realm roles, client roles, and Authorization Services model protected resources. | Applications, tenants, entities, and APIs combine for target services. | Resource servers define custom scopes for user pools. |
| Scopes / permissions / grants | Planned through policy, service-admin, developer-app, and token claims; needs a concrete grant table/story for service-to-resource permissions. | API permissions are granted to M2M apps and emitted in tokens. | Scopes are granted to service apps; Okta API service apps can receive admin roles and scoped grants. | Service accounts receive realm/client roles; optional Authorization Services can model resources and permissions. | Relationships and application roles can model service permissions. | Custom scopes on resource servers are requested in `client_credentials` token calls. |
| Tenant / org scoping | Tenant model exists across platform-admin and tenant-admin; M2M needs explicit tenant-bound service principals and cross-tenant denial defaults. | Supports organization-aware M2M via [Organizations for M2M Applications](https://auth0.com/docs/manage-users/organizations/organizations-for-m2m-applications). | Org is the Okta tenant; service apps are scoped by authorization server, scopes, and admin grants. | Realm is the tenant boundary; service accounts are realm-local unless federation or brokered patterns are added. | Tenant is a core product boundary; applications and entities can be tenant-scoped. | User pool is the primary boundary; app clients and resource servers are pool-scoped. |
| Credential lifecycle | Tables exist for service keys/API keys; needs productized creation, rotation, expiry, revocation, last-used, and one-time reveal semantics. | Client secret lifecycle and credential exchange extension points exist; Actions provide M2M token customization. | Strong private key / JWKS lifecycle for service apps; admin grants are separable from credentials. | Client secrets and keys are managed through admin console/API; rotation is possible but productized differently by realm/client config. | API keys and application secrets are managed through APIs; entity credentials are available through entity APIs. | App client secrets are managed at app-client level; rotation often means creating/updating app clients or secrets. |
| API keys / service keys | Explicit `ApiKey` and `ServiceKey` storage concepts are present; service-admin product should make them first-class alongside OAuth clients. | Auth0 primarily frames M2M as OAuth clients rather than API-key products. | Okta has API tokens and OAuth service apps; strategic path is OAuth service app over static API tokens. | Admin REST access is usually OAuth token based; client secrets can behave like service credentials. | Strong: [API Keys API](https://fusionauth.io/docs/apis/api-keys) is a documented admin surface. | Cognito is OAuth-token oriented; AWS service API keys/IAM are outside Cognito user-pool M2M. |
| Token format / access-token claims | Public token endpoint and JOSE packages provide base; M2M claims need stable `sub`, `client_id`, `azp`, `aud`, `scope`, `tenant`, and service identity rules. | JWT access tokens for custom APIs; claims can be customized with Actions, including M2M trigger. | JWT access tokens from custom authorization servers; Okta API service tokens have Okta-specific scopes/grants. | JWT tokens expose client/service account subject, roles, scopes, audience depending on client configuration. | JWT access tokens and application/entity claims are available through FusionAuth token model. | Access tokens include client/scopes and are validated against Cognito JWKS; see [access tokens](https://docs.aws.amazon.com/cognito/latest/developerguide/amazon-cognito-user-pools-using-the-access-token.html). |
| Resource validation / JWKS / introspection | Dedicated resource-validation API is a strong product boundary; should remain read-only issuer metadata and validation support. | JWKS and token validation are part of Authentication API behavior; management API handles configuration. | JWKS, introspection, and authorization-server metadata are documented OIDC/OAuth surfaces. | Realm OIDC endpoints publish metadata/JWKS and support introspection where enabled. | JWT and introspection endpoints support resource validation. | JWKS and token endpoint metadata support local validation; introspection is not a central Cognito user-pool pattern. |
| Sender-constrained tokens | Gap unless mTLS/DPoP/certificate-bound token semantics are added. | Partial/enterprise-dependent; not the center of Auth0 M2M product framing. | Better positioned due to stronger client auth and policy support, but not the basic M2M default. | Possible with TLS/client policy integration depending on deployment, not the basic service-account pattern. | Not a central M2M feature in public docs. | Gap for Cognito user-pool M2M. |
| Token exchange / delegation | Gap/underframed; useful for workload delegation and service-on-behalf-of chains but should be separate from initial M2M slice. | Auth0 supports token flows and extensibility, but M2M is mostly direct client credentials rather than full delegated workload chains. | Okta supports OAuth/OIDC flows; token exchange support varies by product and should not be assumed for base M2M. | Keycloak supports token exchange in product capabilities, often controlled by realm/client policy. | FusionAuth has JWT APIs and OAuth flows, but workload delegation is not the headline M2M product. | Gap for Cognito user-pool M2M. |
| Audit / telemetry / last-used | Needs explicit audit events for service credential creation, token issuance, rotation, revocation, and last-used. | Logs and Actions can observe M2M token issuance. | System Log records OAuth app and token/admin events. | Admin events and login/events capture client and service-account activity when enabled. | Event log/webhooks can cover API key, auth, and application activity. | CloudTrail covers AWS control plane; Cognito logs/CloudWatch integration varies and is not a full M2M product ledger by default. |
| Admin API surface | Platform-admin and service-admin frontdoors are the intended owner surfaces; storage should be canonical and frontdoors should be thin. | Management API manages clients, APIs, credentials, grants, logs. | Management APIs manage apps, authorization servers, scopes, grants, and roles. | Admin REST API manages realms, clients, service accounts, roles, and scopes. | Application, tenant, entity, relationship, API key, and key APIs manage M2M pieces. | AWS APIs manage user pools, app clients, resource servers, domains, and token configuration. |
| Developer / setup surface | Developer API can own app/client registration; service-admin should own service/workload principals and keys. Need crisp boundary. | Dashboard plus Management API for APIs and M2M apps. | Admin Console plus API for service apps and grants. | Admin Console plus Client Registration Service. | Admin UI plus APIs for applications/entities/API keys. | AWS Console, CloudFormation/CDK/Terraform, and Cognito APIs. |
| UIX / console surface | Planned service-admin UIX and developer UIX; not yet mature. | Dashboard has explicit M2M application and API setup experiences. | Admin Console supports apps, authorization servers, scopes, grants. | Admin Console exposes clients, service accounts, roles, client scopes. | Admin UI exposes applications, tenants, entities, and API keys. | AWS Console exposes app clients, resource servers, domains, and OAuth settings. |
| SDK / examples | Needs first-party examples for service creation, key rotation, `client_credentials`, validation, and token use from a protected API. | SDK/docs examples cover client credentials calls. | SDK/docs examples cover service app JWT assertion and client credentials. | Adapter/docs cover clients and token endpoint usage; service-account examples are common. | Docs include M2M use case and API examples. | AWS SDKs/CLI/CloudFormation support configuration; app code calls token endpoint directly. |
| Main gap / risk | Product is conceptually planned but underframed as an end-to-end workload identity product. Highest-risk gaps: service principal semantics, grant model, credential lifecycle, audit/last-used, and resource validation proof. | Strong product packaging; risk is M2M as OAuth-client centric, with workload identity semantics expressed through applications and APIs. | Strong enterprise service-app story; complexity is split between Okta API service apps, custom authorization servers, scopes, and admin roles. | Powerful and flexible; product surface is admin/realm/client centric rather than packaged as a standalone M2M product. | Broad model with entities plus OAuth/API keys; risk is mapping which primitive is authoritative for each workload pattern. | Good basic `client_credentials` for Cognito APIs; weak compared with full workload identity, delegation, audit, and credential policy products. |

## Interpretation

The vendor pattern is consistent: M2M is not a single endpoint or just OAuth `client_credentials`. It is a cross-surface product spanning authorization-server token issuance, service/client lifecycle, credential management, API/resource permissions, token validation, audit, and an admin/developer setup experience.

For `tigrbl_auth`, the product should be named explicitly as **M2M / Workload Identity**. The `tigrbl-auth-backend-app-service-admin` package is the lifecycle frontdoor for service/workload principals and credentials, not the entire product. The complete M2M product also uses:

| Product layer | tigrbl_auth owner |
| --- | --- |
| Token issuance | `tigrbl-auth-backend-app-public` |
| Token validation and issuer metadata | `tigrbl-auth-backend-app-resource-validation` |
| Service/workload lifecycle | `tigrbl-auth-backend-app-service-admin` |
| Developer app/client setup | `tigrbl-auth-backend-app-developer` |
| Tenant/platform authority boundaries | `tigrbl-auth-backend-app-platform-admin`, `tigrbl-auth-backend-app-tenant-admin` |
| Persistence and canonical tables | `tigrbl-identity-storage` |
| Signing and token crypto | `tigrbl-identity-jose` |
| Authorization decisions | `tigrbl-authz-policy` |

Recommended near-term framing:

1. Treat `Service` or `Workload` as the first-class M2M principal, not merely an OAuth client.
2. Treat `Client` as the protocol-facing credentialed actor that can represent a service principal.
3. Treat `ServiceKey` and `ApiKey` as separate credential products with rotation, expiry, revocation, and audit.
4. Treat API/resource permissions as grants from a service principal/client to a resource server and scope set.
5. Keep resource validation read-only and derived from public issuer state.

## Bibliography

- Auth0: [Client Credentials Flow](https://auth0.com/docs/flows/client-credentials-flow)
- Auth0: [Call Your API Using the Client Credentials Flow](https://auth0.com/docs/get-started/authentication-and-authorization-flow/call-your-api-using-the-client-credentials-flow)
- Auth0: [Machine to Machine Trigger](https://auth0.com/docs/customize/actions/explore-triggers/machine-to-machine-trigger)
- Auth0: [Organizations for M2M Applications](https://auth0.com/docs/manage-users/organizations/organizations-for-m2m-applications)
- Okta: [Implement OAuth for Okta with a service app](https://developer.okta.com/docs/guides/implement-oauth-for-okta-serviceapp/)
- Okta: [Implement the Client Credentials flow](https://developer.okta.com/docs/guides/implement-grant-type/clientcreds/main/)
- Okta: [Client authentication methods](https://developer.okta.com/docs/api/openapi/okta-oauth/guides/client-auth/)
- Okta: [API Access Management](https://developer.okta.com/docs/concepts/api-access-management/)
- Keycloak: [OpenID Connect endpoint layers](https://www.keycloak.org/securing-apps/oidc-layers)
- Keycloak: [Server Administration Guide](https://www.keycloak.org/docs/latest/server_admin/)
- Keycloak: [Authorization Services Guide](https://www.keycloak.org/docs/latest/authorization_services/)
- Keycloak: [Client Registration](https://www.keycloak.org/securing-apps/client-registration)
- FusionAuth: [Machine-to-machine communication](https://fusionauth.io/docs/get-started/use-cases/machine-to-machine)
- FusionAuth: [Entity Management](https://fusionauth.io/docs/get-started/core-concepts/entity-management)
- FusionAuth: [Types and Relationships](https://fusionauth.io/docs/get-started/core-concepts/types-and-relationships)
- FusionAuth: [Application API](https://fusionauth.io/docs/apis/applications)
- FusionAuth: [API Keys API](https://fusionauth.io/docs/apis/api-keys)
- Amazon Cognito: [Define resource servers for your user pool](https://docs.aws.amazon.com/cognito/latest/developerguide/cognito-user-pools-define-resource-servers.html)
- Amazon Cognito: [OAuth grants](https://docs.aws.amazon.com/cognito/latest/developerguide/federation-endpoints-oauth-grants.html)
- Amazon Cognito: [Token endpoint](https://docs.aws.amazon.com/cognito/latest/developerguide/token-endpoint.html)
- Amazon Cognito: [Using the access token](https://docs.aws.amazon.com/cognito/latest/developerguide/amazon-cognito-user-pools-using-the-access-token.html)
