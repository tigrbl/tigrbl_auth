# Authorization Vendor Coverage Matrix

Research date: 2026-05-28

Scope: this matrix compares `tigrbl_auth` with Auth0, Okta, Keycloak, FusionAuth, and Amazon Cognito / Amazon Verified Permissions for ACLs, RBAC, ABAC, DeBAC, PBAC, ReBAC, scopes, tenant isolation, policy lifecycle, and audit.

Authorization vocabulary is inconsistent across vendors. In this matrix:

- ACL means direct subject-to-resource grants or access lists.
- RBAC means roles assigned to users, groups, clients, apps, or service principals.
- ABAC means authorization based on subject, resource, action, or environment attributes.
- DeBAC means delegation-based access control, where authority is valid because a delegator granted it.
- PBAC means policy-based or permission-based access control, depending on vendor wording.
- ReBAC means relationship-based access control or graph authorization.

| Authorization capability | tigrbl_auth | Auth0 | Okta | Keycloak | FusionAuth | Amazon Cognito / Verified Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| Product framing | Policy is a first-class package boundary. Current executable primitives cover RBAC, ABAC, PBAC, delegation, admin policy, traces, and ReBAC-like tests, but product storage/admin surfaces are incomplete. See [Authorization Models Focus](authorization-models-focus.md). | Core Auth0 supports RBAC roles/permissions for APIs, while Auth0 FGA is a separate fine-grained authorization service. See [Auth0 RBAC roles](https://auth0.com/docs/authorization/guides/manage-roles) and [Auth0 FGA docs](https://docs.fga.dev/). | Okta splits admin authorization roles, app/API access policies, groups, scopes, custom admin roles, and FGA under the broader Okta/Auth0 portfolio. See [Okta roles overview](https://developer.okta.com/docs/reference/api/roles/). | Keycloak has realm/client roles, groups, scopes, and Authorization Services for resources, scopes, policies, and permissions. See [Keycloak Authorization Services](https://www.keycloak.org/docs/latest/authorization_services/). | FusionAuth supports application roles, groups, entities/relationships, and FGA integration. See [FusionAuth roles](https://fusionauth.io/docs/get-started/core-concepts/roles) and [FusionAuth docs authorization section](https://fusionauth.io/docs/). | Cognito has groups, scopes, IAM role integration, and resource servers; fine-grained policy decisions are better represented by Amazon Verified Permissions with Cedar. See [Cognito groups](https://docs.aws.amazon.com/cognito/latest/developerguide/cognito-user-pools-user-groups.html) and [Verified Permissions](https://docs.aws.amazon.com/verifiedpermissions/). |
| ACL / direct grants | Planned through `ResourceServerGrant`, delegation grants, relationship tuples, and role assignments; no mature ACL product surface yet. | Auth0 Core is role/permission centric; Auth0 FGA can represent direct object relationships such as user viewer of document. | Okta admin role assignments can target resources; app-level ACLs are usually modeled in applications or FGA. | Authorization Services can model user/resource/scope permissions and direct user policies. | Entity grants and user/application roles can model direct access. | Cognito groups are coarse; Verified Permissions can express direct principal-resource permissions in Cedar. |
| RBAC | Executable `RolePolicy` primitive exists. Needs canonical role, permission, role-permission, and principal-role assignment storage. | Present through API Authorization Core roles and permissions. | Strong for admin roles, custom roles, groups, and app assignments. | Strong realm/client roles and group role mappings. | Strong application-specific roles and group role assignment. | Cognito groups can represent roles and can map to IAM roles; Verified Permissions can also express roles in policies. |
| Group-based authorization | Principal model includes tenant membership/roles; group model is planned but not productized as canonical admin surface. | Auth0 Organizations and roles cover some group-like needs; groups are more prominent in legacy Authorization Extension/FGA models. | Strong group model for assignments, app access, and admin role grants. | Strong groups and group role mappings. | Strong groups, where group membership can dynamically grant application roles. | Cognito user-pool groups manage collections of users and can assign IAM roles. |
| Permission catalog | `PermissionPolicy` primitive exists; needs canonical permission catalog and action naming. | API permissions/scopes can be assigned to roles and emitted in access tokens. | Admin permissions are enumerated for custom roles; scopes and authorization-server policies cover APIs. See [Okta permissions](https://developer.okta.com/docs/api/openapi/okta-management/guides/permissions/). | Authorization Services uses resources, scopes, policies, and permissions. | Application roles are common; FusionAuth docs advise mapping roles to app/API permissions as needed, with FGA for fine-grained cases. | Cognito custom scopes exist on resource servers; Verified Permissions uses actions in Cedar/PARX. |
| OAuth scopes / API authorization | Strong architecture direction: OAuth owns scopes/resource indicators, policy owns scope/permission decisions, resource-server package enforces. | API scopes/permissions and RBAC integrate into access tokens. | API Access Management uses authorization servers, scopes, claims, and access policies. See [Okta policies](https://developer.okta.com/docs/concepts/policies/). | Client scopes and Authorization Services scopes are first-class concepts. | OAuth scopes and application roles are available; API authorization often uses roles/scopes in tokens. | Cognito resource servers define custom scopes; API Gateway/user-pool authorizers can consume tokens. |
| ABAC | Executable `AttributePolicy` primitive exists for required attributes; needs richer operators and normalized subject/resource/context attributes. | Auth0 Core has limited ABAC; Actions/rules/custom claims and FGA contextual modeling can support attribute-driven decisions. | Strong policy conditions using group, device, location, time, network, risk, and app context. | Authorization Services supports claim/attribute policies and JavaScript/custom policies. | User/application/entity data plus lambdas and FGA integration can support ABAC-style decisions. | Verified Permissions is a strong ABAC/PBAC option using principal, action, resource, and context in Cedar. |
| PBAC / policy-based authorization | Executable `PermissionPolicy` and policy-version concepts exist. Needs policy catalog, versioning, parser/evaluator, lifecycle, and admin UI/API. | Auth0 FGA and Actions extend beyond core RBAC; Auth0 Core roles/permissions are not a general PBAC engine. | Okta policies are central for sign-on, API access, app access, authenticators, and management behavior. | Authorization Services is explicitly policy-based with resources, scopes, policies, permissions, and evaluation. | FusionAuth uses roles/groups/entities and APIs; fine-grained policy is typically handled through FGA or application logic. | Verified Permissions is a dedicated PBAC engine using Cedar. |
| DeBAC / delegated administration | Executable `DelegationPolicy` and `AdminPolicy` primitives exist. Needs delegation grants with expiry, revocation, scope, and audit. | Management API roles and organization roles can delegate administration; FGA can model delegated relationships. | Strong admin role assignments to users, groups, and client apps, including custom roles and resource sets. | Realm/client admin roles and fine-grained admin permissions can delegate administration. | Tenant/application roles and admin UI roles cover delegation; entity grants can model non-user relationships. | IAM roles and Cognito groups can delegate AWS resource permissions; Verified Permissions can model app-level delegation in Cedar. |
| ReBAC / graph authorization | Phase 4 tests include `RelationshipGraph`, relationship definitions, tuples, and graph decisions. Needs canonical storage/API. | Strong through Auth0 FGA/OpenFGA relationship tuples and authorization models. See [Auth0 FGA concepts](https://docs.fga.dev/fga-concepts). | Okta/Auth0 FGA provides the strongest ReBAC offering in the portfolio, separate from base Okta IDP admin roles. | Possible through Authorization Services resources/policies, but not a Zanzibar-style tuple system by default. | FusionAuth entity relationships support non-user/entity permission modeling; FGA integration covers deeper graph authorization. See [FusionAuth types and relationships](https://fusionauth.io/docs/get-started/core-concepts/types-and-relationships). | Verified Permissions can model relationships with Cedar entities and policies, though it is policy-language centric rather than tuple-store centric. |
| Resource-level permissions | Planned through `ResourceServer`, resource grants, scopes, permissions, and ReBAC pilot. | Auth0 FGA handles object/resource-level checks; Auth0 Core API permissions are coarser. | Custom admin roles/resource sets and API access policies cover resource scoping; app object-level authorization is often external/FGA. | Strong in Authorization Services with protected resources, scopes, permissions, and policy evaluation. | Entity grants and application roles can model resource-level needs; external FGA handles deeper cases. | Verified Permissions directly supports resource-level authorization with principals, actions, resources, and context. |
| Tenant/org isolation | Tenant boundary is central in architecture and should be first evaluation stage. Needs invariant tests across all frontdoors. | Auth0 tenants and Organizations provide tenant/org context; FGA stores can isolate authorization domains. | Okta org is the primary tenant boundary; groups/apps/policies/resource sets add narrower boundaries. | Realm is the primary boundary. | Tenant is a core boundary; application and entity relationships add sub-boundaries. | User pool is Cognito boundary; Verified Permissions policy stores can isolate authorization domains. |
| Client/service authorization | Policy package covers service identity authorization conceptually; M2M needs resource-server grants and service principal grants. | M2M applications get API permissions/scopes; FGA can authorize service actors where modeled. | Service apps can receive scopes/admin roles; client apps can be principals for admin role assignments. | Service accounts can receive roles; Authorization Services can evaluate client/resource permissions. | Applications, entities, and API keys support service-style access; entity relationships can model service authorization. | Cognito app clients and scopes support M2M; Verified Permissions can authorize machine principals if modeled. |
| Contextual/risk policy | Phase 4 advanced identity tests include context such as MFA, device, country, time, anomaly. Needs productized normalized context. | Actions and tenant security features can apply contextual logic; FGA conditions can cover some contextual cases. | Strong contextual policy posture through policies over user, group, device, location, time, network, and risk. | Conditional flows and custom policies can include context/claims. | Lambdas/user data and app logic can support context; less centralized than Okta policy. | Verified Permissions supports request context in Cedar; Cognito itself is less expressive. |
| Deny/precedence model | Underframed. Needs explicit default deny, deny override, conflict resolution, and trace semantics. | Core RBAC is allow/permission oriented; FGA check semantics depend on model. | Policy rule priority and conditions are core concepts. | Authorization Services has decision strategies and policy evaluation semantics. | App logic/FGA integration must define precedence. | Cedar policies can include permit/forbid semantics and policy analysis. |
| Policy language | Phase 4 tests publish policy source and reject unsafe constructs; no accepted language contract yet. | Auth0 FGA has an authorization-model DSL for relationships. | Okta policies are configured through product APIs rather than a general external policy language; Okta FGA uses the FGA model language. | Authorization Services supports policy types including JavaScript/custom policies depending on deployment. | No single first-party general policy language in core FusionAuth; FGA/external integration fills this. | Cedar is a first-class policy language for Verified Permissions. |
| Decision API / check endpoint | Internal policy primitives exist; needs shared decision API consumed by every frontdoor. | Auth0 FGA provides check APIs; Auth0 Core relies on token roles/permissions and Management API. | Okta policy decisions are embedded in platform flows; FGA provides external check APIs where used. | Authorization Services provides entitlement/authorization evaluation patterns and policy enforcer integrations. | FusionAuth APIs provide roles/groups/entities; external FGA or app code often performs checks. | Verified Permissions provides authorization APIs such as IsAuthorized / token-based authorization. |
| Decision trace / audit | Executable traces exist; need persisted redacted decision logs and admin/operator views. | Auth0 logs and FGA logging/observability exist across products. | System Log and policy/admin logs cover changes and access decisions depending on surface. | Admin events, user events, and authorization debugging/evaluation tools exist. | Event logs/webhooks cover auth/admin changes; authorization decision logging depends on integration. | CloudTrail covers AWS API/control-plane; Verified Permissions has service logs/integration telemetry depending on setup. |
| Admin/API surface | Needs canonical storage and frontdoor routes for roles, permissions, policies, assignments, delegation, and decisions. | Dashboard and Management API manage RBAC roles/permissions; FGA has its own APIs for models/tuples/checks. | Admin Console and Management APIs manage roles, groups, policies, resource sets, authorization servers, and scopes. | Admin Console and Admin REST manage roles/groups/authz resources/scopes/policies/permissions. | Admin UI and APIs manage applications, roles, groups, users, entities, and API keys. | AWS Console/APIs manage Cognito groups/resource servers; Verified Permissions APIs manage policy stores, schemas, policies, and authorization checks. |
| Main gap / risk | Current primitives are promising, but product maturity requires canonical storage, policy lifecycle, decision API, persisted traces, frontdoor integration, and resource-server grant enforcement. | Split-brain risk between Auth0 Core RBAC and Auth0 FGA unless product boundaries are clear. | Powerful but distributed across admin roles, policies, API access management, and FGA portfolio surfaces. | Very flexible but configuration-heavy; resource authorization can be operationally complex. | Clear app roles/groups, but deep authorization usually moves to entity relationships or FGA/app logic. | Cognito alone is basic for app authorization; serious PBAC/ABAC/ReBAC should use Verified Permissions. |

## Interpretation

The competitor pattern is not one authorization model. It is layered:

| Layer | Common vendor product shape |
| --- | --- |
| Basic app authorization | Roles, groups, app roles, scopes, token claims. |
| Admin authorization | Admin roles, custom roles, resource sets, tenant/org authority, delegated admin. |
| API authorization | Resource servers, scopes, access policies, token claims, gateway/resource-server enforcement. |
| Fine-grained authorization | Separate FGA/PBAC/ReBAC service or module with authorization models, relationship tuples, policies, and check APIs. |
| Audit and operations | Logs for policy changes, assignment changes, role changes, and authorization decisions where supported. |

For `tigrbl_auth`, the target product shape should be:

| Product layer | Recommended owner |
| --- | --- |
| Authorization decision primitives | `tigrbl-authz-policy` |
| Principal facts and memberships | `tigrbl-identity-principals` |
| Credential and assurance facts | `tigrbl-authn-credentials` |
| OAuth scopes and resource indicators | `tigrbl-auth-protocol-oauth` |
| Resource-server grants and enforcement | `tigrbl-authz-resource-server`, `tigrbl-auth-backend-app-resource-validation` |
| Canonical role/policy/grant/relationship tables | `tigrbl-identity-storage` |
| Admin lifecycle | Platform-admin, tenant-admin, developer, and service-admin frontdoors as scoped Management API slices |
| Policy UI | Platform-admin and tenant-admin UIX first, developer/service-admin UIX where delegated |

Recommended near-term stance:

1. Keep RBAC, ABAC, PBAC, DeBAC, ReBAC, scopes, and tenant isolation as composed decision inputs.
2. Start productization with permission catalog, roles, assignments, delegation grants, and decision traces.
3. Treat resource-server grants as the bridge between OAuth scopes and policy decisions.
4. Add ReBAC after the storage and decision API are stable.
5. Avoid claiming full fine-grained authorization until policy lifecycle, decision API, storage, and audit are wired through frontdoors.

## Bibliography

- Auth0: [Manage Role-Based Access Control Roles](https://auth0.com/docs/authorization/guides/manage-roles)
- Auth0: [Auth0 Fine-Grained Authorization Docs](https://docs.fga.dev/)
- Auth0 FGA: [Fine-Grained Authorization Concepts](https://docs.fga.dev/fga-concepts)
- Auth0: [Fine-Grained Authorization at scale](https://auth0.com/developers/lab/fine-grained-authorization)
- Okta: [Roles overview](https://developer.okta.com/docs/reference/api/roles/)
- Okta: [Permissions](https://developer.okta.com/docs/api/openapi/okta-management/guides/permissions/)
- Okta: [Policies](https://developer.okta.com/docs/concepts/policies/)
- Keycloak: [Authorization Services](https://www.keycloak.org/docs/latest/authorization_services/)
- FusionAuth: [Roles](https://fusionauth.io/docs/get-started/core-concepts/roles)
- FusionAuth: [Groups](https://fusionauth.io/docs/get-started/core-concepts/groups)
- FusionAuth: [Types and Relationships](https://fusionauth.io/docs/get-started/core-concepts/types-and-relationships)
- Amazon Cognito: [Adding groups to a user pool](https://docs.aws.amazon.com/cognito/latest/developerguide/cognito-user-pools-user-groups.html)
- Amazon Cognito: [Scopes, M2M, and resource servers](https://docs.aws.amazon.com/cognito/latest/developerguide/cognito-user-pools-define-resource-servers.html)
- Amazon Verified Permissions: [Documentation](https://docs.aws.amazon.com/verifiedpermissions/)
- Amazon Verified Permissions: [What is Amazon Verified Permissions](https://docs.aws.amazon.com/verifiedpermissions/latest/userguide/what-is-avp.html)
