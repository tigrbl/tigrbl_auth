services:
  tigrbl-auth:
    build:
      context: .
      dockerfile: Dockerfile.dev-tigrbl
    image: tigrbl-auth-local:latest
    container_name: tigrbl-auth-local
    ports:
      - "8001:8000"
    environment:
      TIGRBL_AUTH_INSTALL_PROFILE: tigrcorn
      AUTHN_ISSUER: http://localhost:8001
      TIGRBL_AUTH_PROTECTED_RESOURCE_IDENTIFIER: http://localhost:8001/resource
      TIGRBL_AUTH_REQUIRE_TLS: "false"
      PG_HOST: ""
      PG_PORT: "5432"
      PG_DB: ""
      PG_USER: ""
      PG_PASS: ""
      REDIS_HOST: ""
      REDIS_PASSWORD: ""
    restart: unless-stopped
