Facade compatibility boundary T0 inventory passed.

`tests/unit/test_facade_compatibility_boundary.py` imports the package-owned compatibility manifest, stable entrypoint map, extras map, warning class, and legacy facade modules under an isolated facade-only import path.
