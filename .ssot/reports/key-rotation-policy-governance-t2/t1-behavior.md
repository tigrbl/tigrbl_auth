Key rotation policy governance boundary T1 behavior passed.

`tests/unit/test_key_rotation_policy_boundary.py` creates, approves, publishes, views, and executes a policy-bound key rotation through the JOSE package runtime surface, proving policy version binding, effective publication, authorization references, JWKS publication evidence, retirement evidence, and audit recording.
