# Identity package-suite migration completion plan

Status: planned.

The canonical layer index and ownership checks are implemented, but the broader
identity package-suite feature remains partial until all packages are owned by
their intended layers, transitional compatibility surfaces are retired, and the
package dependency exception ledger contains no identity-suite migration debt.

Completion requires an executable zero-migration-debt assertion in addition to
the existing canonical classification and dependency-direction tests.
