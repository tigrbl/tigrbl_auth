Provider white-label login boundary T2 gate passed.

`tests/unit/test_provider_white_label_login_boundary.py` verifies unsafe asset URI rejection, disallowed asset extension rejection, unsafe path rejection, invalid color rejection, cross-tenant branding mismatch rejection, missing tenant branding rejection, and provider import boundary cleanliness.
