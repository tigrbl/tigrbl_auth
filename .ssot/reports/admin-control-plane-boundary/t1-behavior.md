Admin control-plane boundary T1 behavior passed.

`tests/unit/test_admin_control_plane_boundary.py` verifies CRUD behavior for principals, credentials, apps, service identities, resource servers, policies, and admin UIX state projection.
