# Package maturity governance T0 inventory evidence

Runtime proof: `tests/unit/test_package_maturity_governance.py::test_package_maturity_t0_discovers_all_split_packages`.

This verifies that all split package distributions are discoverable from repository package metadata, expose independent import roots, and report the current package version.
