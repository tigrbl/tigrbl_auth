Phase 1 surface boundary T0 inventory passed.

`tests/unit/test_phase1_surface_boundary.py` inventories the runtime surface-boundary manifest for all 16 frozen features, including admin UIX, public UIX, admin API, public API, applicability classification, OAuth, OIDC, discovery, registration, logout, and admin/public exclusion guards.
