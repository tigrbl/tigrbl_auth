OAuth and OIDC protocol boundary T0 inventory passed.

`tests/unit/test_oauth_oidc_protocol_boundary.py` imports package-owned OAuth and OIDC provider runtime surfaces from independent package roots.
