OAuth and OIDC protocol boundary T2 gate passed.

`tests/unit/test_oauth_oidc_protocol_boundary.py` verifies replay rejection, invalid scope and actor denial, mTLS mismatch denial, redirect and asset policy rejection, session mismatch rejection, and provider/RP import isolation.
