Phase 2 public UIX browser security T2 gate passed.

`apps/public-uix/services/publicBoundary.test.ts` verifies fail-closed behavior for malformed sessions, unsafe redirects, admin hash routes, cross-origin endpoints, RPC/control-plane endpoint discovery, unsafe token disclosure, unsafe error disclosure, and unconstrained redirect rows.
