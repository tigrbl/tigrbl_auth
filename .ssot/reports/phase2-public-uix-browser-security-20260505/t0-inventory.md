Phase 2 public UIX browser security T0 inventory passed.

`apps/public-uix/services/publicBoundary.test.ts` inventories all 15 public UIX boundary features through the runtime `publicUixBoundaryManifest`, including shell, auth session, login, logout, registration, recovery, continuity, OpenAPI consumption, token handling, problem details, safe errors, consent, cookie sessions, CSRF, and origin/redirect constraints.
