# Principals Model Boundary T0 Inventory

`tests/unit/test_principals_model_boundary.py` imports the package root and constructs user, admin, service, app, machine, workload, device, membership, and alias objects.
