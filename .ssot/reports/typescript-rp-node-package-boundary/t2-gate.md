TypeScript RP node package boundary T2 gate passed.

`npm run build -w @tigrbl-auth/rp` verifies strict TypeScript declarations and browser library build output, while `tests/unit/test_typescript_rp_node_package_boundary.py` verifies Node matrix and npm provenance workflow wiring.
