TypeScript RP node package boundary T1 behavior passed.

`npm run test -w @tigrbl-auth/rp` verifies browser-safe RP runtime behavior: PKCE authorization URL construction, callback parsing, state validation, discovery validation, ID token claim validation, logout URL construction, memory session handling, Node matrix manifest, and npm provenance manifest.
