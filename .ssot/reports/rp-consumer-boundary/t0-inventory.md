RP consumer boundary T0 inventory passed.

`tests/unit/test_rp_consumer_boundary.py` imports package-owned RP models, authorization URL, PKCE, state, callback, browser, server, discovery, JWKS, UserInfo, logout, and adapter surfaces.
