# Published-runner verification checkpoint: tigrcorn pin re-verified against PyPI on 2026-03-24.
# Tigrcorn runner profile pins.
# Use with: pip install -c constraints/base.txt -c constraints/runner-tigrcorn.txt '.[tigrcorn]'
# Tigrcorn upstream requires Python >=3.11, so this profile participates in the
# certification matrix on Python 3.11 and 3.12 only.

tigrcorn==0.3.8 ; python_version >= '3.11'
